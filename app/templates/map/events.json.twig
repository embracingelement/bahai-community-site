[
    {% for neighborhood in  activityType.neighborhoods %}
        {
            "id" : "{{ neighborhood.getId() }}",
            "location" : "{{ neighborhood.location }}",
            "type": "{{ activityType.getId() }}",
            "count" :{{ neighborhood.events | length }}
        },
    {% endfor %}
]